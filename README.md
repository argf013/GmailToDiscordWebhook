# GmailToDiscordWebhook
This script uses Google Apps Script to check for new emails in a Gmail inbox and send notifications to a Discord channel using a webhook. It also sets up a time-based trigger to check for new emails every minute.
